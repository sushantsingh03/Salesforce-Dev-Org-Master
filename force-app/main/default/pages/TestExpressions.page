<apex:page >
    <apex:pageBlock >
       Comment - Formula Expressions example - <br/>
        
     	<p>Today's date is {! today() }</p> 
        <p>Next week it will be {! today()+7 }</p>
        <p>The year today is {! YEAR(TODAY()) }</p>
		<p>Tomorrow will be day number  {! DAY(TODAY() + 1) }</p>
		<p>Let's find a maximum: {! MAX(1,2,3,4,5,6,5,4,3,2,1) } </p>
		<p>The square root of 49 is {! SQRT(49) }</p>
        <p>Is it true?  {! CONTAINS('salesforce.com', 'force.com') }</p><br/>
        
        Comment - Conditional Expressions - <br/>
        
        <p>
            {! if(contains ('sushant singh','sushant'),'Contains Sushant', 'Does not contain sushant')}
        </p>
        <p>
            {! if(year(today())>2018, year(today()), 2018 )}
        </p>
    
    
    
    </apex:pageBlock>
</apex:page>